from setuptools import setup, find_packages

setup(
    name="alphalib_missions",
    version="0.1.0",
    description="AlphaLib Missions - lettre de mission, onboarding et espace documentaire",
    author="AlphaLib",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
